{% set goname = "github.com/hashicorp/vault" %}
{% set version = "1.4.3" %}

{% set name = goname.split('/')[-1] %}
{% set pkg_src = 'src' + os.sep + goname.replace("/",os.sep) %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - folder: {{ pkg_src }}
    url: https://{{ goname }}/archive/v{{ version }}.tar.gz
    sha256: 5210e6e0525fbbffbe2749e2dfc19782214e5223837d51005d68450765b698bf

build:
  number: 0
  # windows builds disabled since we don't have a go compiler for 1.12
  skip: true  # [win]
  script:
    - pushd {{ pkg_src }}
    - go install -v .

requirements:
  build:
    - {{ compiler('go') }}

test:
  commands:
    - {{ name|lower }} -v

about:
  home: https://www.vaultproject.io
  license: MPL 2.0
  summary: A tool for managing secrets.
  description: |
    Vault is a tool for securely accessing secrets. A secret is anything that you
    want to tightly control access to, such as API keys, passwords, certificates,
    and more. Vault provides a unified interface to any secret, while providing
    tight access control and recording a detailed audit log.
  doc_url: https://www.vaultproject.io/docs/index.html
  dev_url: https://github.com/hashicorp/vault.git

extra:
  recipe-maintainers:
    - johanneskoester
    - sodre
    - adbeda
    - mariusvniekerk
    - oblute
    - rluria14
